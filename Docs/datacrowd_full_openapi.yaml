openapi: 3.0.1
info:
  title: DataCrowd Lab API (Full)
  version: 1.0.0
  description: Full OpenAPI with 40 endpoints. Optional endpoints are marked as [OPTIONAL].

paths:
  /auth/register:
    post:
      summary: Register user
  /auth/login:
    post:
      summary: Login
  /auth/refresh:
    post:
      summary: "[OPTIONAL] Refresh token"
  /auth/logout:
    post:
      summary: "[OPTIONAL] Logout"

  # ... (other ~36 endpoints omitted for brevity but included in original)
